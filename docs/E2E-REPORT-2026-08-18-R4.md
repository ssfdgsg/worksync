# worksync E2E 回归报告 R4（2026-08-18）

## 1. 结论

本轮同时验证完整同步链路、直接 fast-forward pull 和“远端不保存 restic 密码”的安全模型。

结果：**功能主链路全部通过，上一轮 E2E-012 已修复；远端密码同域托管问题也已修复。**

关键成功标记：

```text
remote-password-absent
secure-full-restore-ok
direct-fast-forward-ok
```

当前发布判断：核心功能可以进入更大规模试用。仍建议在发布前增加并发 push、远端对象损坏和大仓库性能测试。

## 2. 测试环境

| 项目 | 值 |
|---|---|
| 基线 | `8d67055` + 当前工作区未提交修复 |
| Go | go1.24.10 darwin/arm64 |
| Podman | 5.8.1，rootless Podman Machine |
| restic | 0.18.0 |
| SSH/SFTP | 临时用户态 sshd，loopback 自定义端口 |
| Binary | `/tmp/worksync-e2e-r4` |
| Project | `e2e-r4` |

`go test ./...` 全部通过。

## 3. 完整 E2E

### 3.1 source commit 与 push

- 全新 source data/config 直接 `up` 成功。
- 写入 rootfs 与 tracked workspace 标记。
- commit 成功：`sha256:f2fafcabe62f00c5e6a3ea038d7fda1f26073c1328bdbd5b8f17fd73405801bc`。
- 本地自动生成随机 restic 密码，权限为 `0600`。
- SSH/SFTP push 成功，上传 12 个对象。
- 远端明确验证不存在独立 `restic-password` 文件。

### 3.2 fresh clone 与带外密码

fresh clone 无 state DB、OCI、restic repository 和密码文件：

1. `pull origin` 成功下载提交与加密仓库对象。
2. 未提供密码时，`rollback latest` 安全失败并明确提示使用环境变量或本地密码文件。
3. 通过本地 `0600` 文件带外提供密码，不经过 Git/远端对象仓库。
4. rollback 成功恢复 workspace。
5. up 从提交镜像创建容器。
6. rootfs/workspace 同时验证成功，输出 `secure-full-restore-ok`。

### 3.3 双机增量与直接 fast-forward

clone 创建第二提交：

```text
sha256:912b62337b1f71a45eda1d2fdb74919e843f273414f2ac7c477c412573522276
```

- clone 增量 push 成功，上传 9 个对象。
- source 停留在第一提交，没有预先执行 fetch。
- source 直接 `pull origin` 成功，下载 1 个提交。
- source rollback + up 后，第二提交的 rootfs/workspace 标记均通过。
- 最终输出 `direct-fast-forward-ok`。

说明 E2E-012 的“先下载 descriptor，再判断祖先关系”修复有效。

## 4. 安全审计与清理

### 4.1 当前 Git 工作树

- 未发现已跟踪的 SSH 私钥、PEM 私钥、restic 密码文件或 `.pm` 测试状态。
- `.pm/` 已整体忽略。
- 增加 `.env`、`.ssh`、PEM/key 和常见 SSH 私钥文件的 Git ignore 规则。
- 文档中的个人绝对路径已替换为 `$REPO`/`$E2E_ROOT`。
- 文档示例用户名统一替换为 `dev`。
- 高置信 token/private-key 模式扫描未命中真实凭据。

### 4.2 本地测试敏感文件

测试结束后已删除：

- 临时 SSH client/host 私钥；
- R3/R4 source 与 clone 的 restic 本地密码文件；
- R3 旧实现曾上传到测试远端的明文密码元数据。

删除后，对应 R3/R4 测试 restic 仓库无法再解密。这些均为 Git 忽略目录下的临时测试数据，不影响源码。

### 4.3 Git 历史

历史补丁扫描没有发现高置信密钥或 token；但早期提交中包含开发机个人绝对路径。当前工作树已脱敏，历史尚未重写。

重写历史会改变 commit ID 并要求所有协作者重新同步，因此本轮没有自动执行。如果仓库已经公开且个人路径也要求从历史彻底移除，应单独使用 `git filter-repo` 清理并 force push。

## 5. 后续建议

1. 对同一 remote 执行并发 push，验证 ref CAS 与 restic 对象并集合并。
2. 人为截断远端 pack/index/archive，验证完整性检测和修复策略。
3. 使用包含大量小文件和多 GB pack 的仓库测试逐文件 SFTP 性能。
4. 为 restic 密码增加系统 keychain/secret-command 集成，避免用户手工复制密码文件。

## 6. 测试卫生

- 测试容器已删除。
- 临时 sshd 已关闭。
- 产品改动是测试开始前已有的用户改动，未被覆盖。
- 本轮新增文档与 Git 脱敏修改均不包含真实凭据。

